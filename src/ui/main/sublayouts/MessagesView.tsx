import { TitleHeader } from "../../../components/layout/TitleHeader"

export const MessagesView = ()=>{
    return <div className="view messages">
        <TitleHeader title={"Messages"} />
    </div>
}