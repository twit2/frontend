import { TitleHeader } from "../../../components/layout/TitleHeader"

export const NotificationsView = ()=>{
    return <div className="view notifications">
        <TitleHeader title={"Notifications"} />
    </div>
}